// MUSIC SETTINGS
switch(global.musicVolumeSetting)
{
    case 10:
    {
        // Menu
        audio_sound_gain(menu_song, 1, 0);
        audio_sound_gain(dark_chill, 1, 0);
        audio_sound_gain(level_song, 1, 0);
        audio_sound_gain(level_xploration, 1, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 1, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 1, 0);
        audio_sound_gain(wakeUp, 1, 0);
        audio_sound_gain(mus_Gluttony, 1, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 1, 0);
        audio_sound_gain(mus_SlothMain, 1, 0);
        // More Music
        break;
    }
    case 9:
    {
        // Menu
        audio_sound_gain(menu_song, 0.9, 0);
        audio_sound_gain(dark_chill, 0.9, 0);
        audio_sound_gain(level_song, 0.9, 0);
        audio_sound_gain(level_xploration, 0.9, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.9, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.9, 0);
        audio_sound_gain(wakeUp, 0.9, 0);
        audio_sound_gain(mus_Gluttony, 0.9, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.9, 0);
        audio_sound_gain(mus_SlothMain, 0.9, 0);
        // More Music
        break;
    }   
    case 8:
    {
        // Menu
        audio_sound_gain(menu_song, 0.8, 0);
        audio_sound_gain(dark_chill, 0.8, 0);
        audio_sound_gain(level_song, 0.8, 0);
        audio_sound_gain(level_xploration, 0.8, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.8, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.8, 0);
        audio_sound_gain(wakeUp, 0.8, 0);
        audio_sound_gain(mus_Gluttony, 0.8, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.8, 0);
        audio_sound_gain(mus_SlothMain, 0.8, 0);
        // More Music
        break;
    }   
    case 7:
    {
        // Menu
        audio_sound_gain(menu_song, 0.7, 0);
        audio_sound_gain(dark_chill, 0.7, 0);
        audio_sound_gain(level_song, 0.7, 0);
        audio_sound_gain(level_xploration, 0.7, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.7, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.7, 0);
        audio_sound_gain(wakeUp, 0.7, 0);
        audio_sound_gain(mus_Gluttony, 0.7, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.7, 0);
        audio_sound_gain(mus_SlothMain, 0.7, 0);
        // More Music
        break;
    }   
    case 6:
    {
        // Menu
        audio_sound_gain(menu_song, 0.6, 0);
        audio_sound_gain(dark_chill, 0.6, 0);
        audio_sound_gain(level_song, 0.6, 0);
        audio_sound_gain(level_xploration, 0.6, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.6, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.6, 0);
        audio_sound_gain(wakeUp, 0.6, 0);
        audio_sound_gain(mus_Gluttony, 0.6, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.6, 0);
        audio_sound_gain(mus_SlothMain, 0.6, 0);
        // More Music
        break;
    }   
    case 5:
    {
        // Menu
        audio_sound_gain(menu_song, 0.5, 0);
        audio_sound_gain(dark_chill, 0.5, 0);
        audio_sound_gain(level_song, 0.5, 0);
        audio_sound_gain(level_xploration, 0.5, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.5, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.5, 0);
        audio_sound_gain(wakeUp, 0.5, 0);
        audio_sound_gain(mus_Gluttony, 0.5, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.5, 0);
        audio_sound_gain(mus_SlothMain, 0.5, 0);
        // More Music
        break;
    }   
    case 4:
    {
        // Menu
        audio_sound_gain(menu_song, 0.4, 0);
        audio_sound_gain(dark_chill, 0.4, 0);
        audio_sound_gain(level_song, 0.4, 0);
        audio_sound_gain(level_xploration, 0.4, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.4, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.4, 0);
        audio_sound_gain(wakeUp, 0.4, 0);
        audio_sound_gain(mus_Gluttony, 0.4, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.4, 0);
        audio_sound_gain(mus_SlothMain, 0.4, 0);
        // More Music
        break;
    }   
    case 3:
    {
        // Menu
        audio_sound_gain(menu_song, 0.3, 0);
        audio_sound_gain(dark_chill, 0.3, 0);
        audio_sound_gain(level_song, 0.3, 0);
        audio_sound_gain(level_xploration, 0.3, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.3, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.3, 0);
        audio_sound_gain(wakeUp, 0.3, 0);
        audio_sound_gain(mus_Gluttony, 0.3, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.3, 0);
        audio_sound_gain(mus_SlothMain, 0.3, 0);
        // More Music
        break;
    }   
    case 2:
    {
        // Menu
        audio_sound_gain(menu_song, 0.2, 0);
        audio_sound_gain(dark_chill, 0.2, 0);
        audio_sound_gain(level_song, 0.2, 0);
        audio_sound_gain(level_xploration, 0.2, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.2, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.2, 0);
        audio_sound_gain(wakeUp, 0.2, 0);
        audio_sound_gain(mus_Gluttony, 0.2, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.2, 0);
        audio_sound_gain(mus_SlothMain, 0.2, 0);
        // More Music
        break;
    }   
    case 1:
    {
        // Menu
        audio_sound_gain(menu_song, 0.1, 0);
        audio_sound_gain(dark_chill, 0.1, 0);
        audio_sound_gain(level_song, 0.1, 0);
        audio_sound_gain(level_xploration, 0.1, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0.1, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0.1, 0);
        audio_sound_gain(wakeUp, 0.1, 0);
        audio_sound_gain(mus_Gluttony, 0.1, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0.1, 0);
        audio_sound_gain(mus_SlothMain, 0.1, 0);
        // More Music
        break;
    }   
    case 0:
    {
        // Menu
        audio_sound_gain(menu_song, 0, 0);
        audio_sound_gain(dark_chill, 0, 0);
        audio_sound_gain(level_song, 0, 0);
        audio_sound_gain(level_xploration, 0, 0)
        //Save and Death
        audio_sound_gain(mus_sleeping, 0, 0)
        // Gluttony
        audio_sound_gain(wakeUpToybox, 0, 0);
        audio_sound_gain(wakeUp, 0, 0);
        audio_sound_gain(mus_Gluttony, 0, 0);
        // Sloth Music
        audio_sound_gain(mus_Sloth, 0, 0);
        audio_sound_gain(mus_SlothMain, 0, 0);
        // More Music
        break;
    }      
}  
